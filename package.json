{
  "name": "get-started-react",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@types/react": "^16.0.19",
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-scripts": "1.0.16"
  },
  "scripts": {
    "start": "react-scripts start",
    "start-host": "HOST=127.0.0.1 react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "commit-dev": "git add . && git commit -m update && git push origin develop"

  }
}
